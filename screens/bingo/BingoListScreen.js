import { StyleSheet, Text, SafeAreaView } from "react-native";
import React from "react";

const BingoListScreen = () => {
  return (
    <SafeAreaView>
      <Text>BingoListScreen</Text>
    </SafeAreaView>
  );
};

export default BingoListScreen;

const styles = StyleSheet.create({});
