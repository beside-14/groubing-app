import { StyleSheet, Text, SafeAreaView } from "react-native";
import React from "react";

const MyLevelScreen = () => {
  return (
    <SafeAreaView>
      <Text>MyLevelScreen</Text>
    </SafeAreaView>
  );
};

export default MyLevelScreen;

const styles = StyleSheet.create({});
